from scripts.constants.app_configuration import BASE_URL

class Endpoints:
    base_url = "/profiler/"
    heart_beat = base_url + "heart_beat"


class Commons:
    status = "status"
    message = "message"
    success = "success"
    failed = "failed"

